apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: test-service4
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/generalsanjeet/gitops-repo
    targetRevision: HEAD
    path: apps/test-service4/helm
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
